# Virtual Network Module

This module defines a Virtual Network and a Subnet in Azure.

## Resources Created
1. Virtual Network (`azurerm_virtual_network`)
2. Subnet (`azurerm_subnet`)

## Inputs
- `resource_group_name`: Name of the Resource Group.
- `location`: Azure region (default: `West Europe`).
- `vnet_name`: Name of the Virtual Network (default: `thesis-vnet`).
- `address_space`: IP range for the Virtual Network (default: `10.0.0.0/16`).
- `subnet_name`: Name of the Subnet (default: `thesis-subnet`).
- `subnet_address_prefixes`: IP range for the Subnet (default: `10.0.1.0/24`).

## Outputs
- `vnet_name`: Name of the Virtual Network.
- `vnet_id`: ID of the Virtual Network.
- `subnet_name`: Name of the Subnet.
- `subnet_id`: ID of the Subnet.
